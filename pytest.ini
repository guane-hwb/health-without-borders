[pytest]
pythonpath = .
testpaths = tests
addopts = --cov=app --cov-report=term-missing
filterwarnings =
    ignore::DeprecationWarning
markers =
    pg: needs PostgreSQL (tests/integration; skipped without a PostgreSQL DATABASE_URL)
